# Required
VAULT_PASSWORD=your-password-here

# Optional — for in-browser editor GitHub commits
GITHUB_TOKEN=
GITHUB_REPO=

# Optional — for x402 endpoints (add keys for your upstream data sources)
FIRECRAWL_API_KEY=
# Add domain-specific API keys below
